akhq:
  security:
    groups:
      admin:
        roles:
          - topic/read
      limited:
        roles:
          - topic/read
      extra:
        roles:
          - topic/read
          - topic/write

    header-auth:
      user-header: x-akhq-user
      groups-header: x-akhq-group
      users: []